# This repository carries the bot and nothing else.
#
# Rather than listing what stays out, everything is ignored and the
# three files are re-admitted by name. Nothing new can be added without
# being listed here — not a credential, not a working copy, not a
# generated file someone forgot to exclude.
*

!.gitignore
!unitinfo.ts
!messages.js
!logic.generated.js
